# Server-side origin used by the Next.js /v1 rewrite.
XIAOAN_API_ORIGIN=http://127.0.0.1:8000

# Development only. Keep false in normal and production deployments.
NEXT_PUBLIC_ENABLE_DEBUG_EXPORT=false
NEXT_PUBLIC_ENABLE_CHAT_DEBUG=false
